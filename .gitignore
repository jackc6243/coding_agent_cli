node_modules
dist
.env
*.log
.DS_Store

chroma*
.claude*
/test_repo/*

# Never ignore anything in src directory (except specific exceptions below)
!src/**

# MCP servers workspace artifacts (exceptions to the src rule above)
src/mcp/servers/**/build
src/mcp/servers/**/node_modules